node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
# Variables de entorno (credenciales) — nunca al repo
.env
.env.*
!.env.example
.vercel
next-env.d.ts

/lib/generated/prisma

# Imágenes de productos descargadas de Drive (no se suben al repo)
/fotos
/scripts/seed-report.json

# Artefactos generados / binarios que no van al repo
tsconfig.tsbuildinfo
*.exe
cloudflared*
# Log que deja el túnel de cloudflared al correrlo en local
cloudflare.log
# Imágenes fuente de diseño (se suben a Cloudinary, no al repo)
treeHero*
HeroMovil*
NuestraHistoria*
